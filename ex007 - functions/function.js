function parimp(n) {
  if (n % 2 == 0) {
    return 'par';
  } else {
    return 'impar';
  }
}

console.log(parimp(9));

// -------------------------------------------------------------------- //

function soma(n1, n2) {
  return n1 + n2;
}

console.log(soma(432894234, 32490324))

// -------------------------------------------------------------------- //

const res = (x) => {
  return x*2
}

console.log(res(10))